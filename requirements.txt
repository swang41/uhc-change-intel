# Core (always required)
pypdf>=4.0
pdfminer.six>=20221105
tiktoken>=0.6
numpy>=1.26
sentence-transformers>=3.0   # local dense embeddings (all-MiniLM-L6-v2)

# LLM generation — local mode uses Gemini REST API via urllib (stdlib, no extra dep)
# Set GEMINI_API_KEY in config/.env (free key at https://aistudio.google.com/apikey)

# GCP backend (only needed for --mode gcp)
google-cloud-aiplatform>=1.50
vertexai>=1.50

# Config
python-dotenv>=1.0

# Dev / eval
pytest>=8.0
